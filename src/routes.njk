---
layout: layout.njk
pagination:
  data: routes
  size: 1
  alias: r
permalink: "/{{ r.citySlug }}/{{ r.airportSlug }}/{{ r.districtSlug }}/{{ r.areaSlug }}/"

eleventyComputed:
  title: "{{ r.cityName }} {{ r.airportName }} {{ r.districtName }} {{ r.areaName }} Airport Transfer"
  description: "Private airport transfer from {{ r.airportName }} to {{ r.districtName }} {{ r.areaName }} in {{ r.cityName }}. Duration, pricing and booking information."
---

{% set seed = r.citySlug + r.districtSlug + r.areaSlug %}

<nav class="breadcrumb">
  <a href="{{ '/' | url }}">Home</a>
  <span>›</span>

  <a href="{{ '/' | url }}{{ r.citySlug }}/">
    {{ r.cityName }}
  </a>
  <span>›</span>

  <span>{{ r.districtName }} {{ r.areaName }}</span>
</nav>


<h1>{{ r.cityName }} {{ r.airportName }} → {{ r.districtName }} {{ r.areaName }} Airport Transfer</h1>

<p class="lead">
  {{ utils.render(utils.pick(seo.intro, seed), r) }}
</p>

<h2>Transfer Duration</h2>
<p>
  {{ utils.render(utils.pick(seo.duration, seed), r) }}
</p>

<h2>Airport Transfer Pricing</h2>
<p>
  {{ utils.render(utils.pick(seo.pricing, seed), r) }}
</p>

<h2>Why Choose Private Airport Transfer?</h2>
<ul>
  <li>Flight tracking and punctual pickup</li>
  <li>Door-to-door private service</li>
  <li>Fixed pricing with no hidden costs</li>
  <li>Professional drivers and modern vehicles</li>
</ul>

<h2>About {{ r.districtName }} {{ r.areaName }}</h2>
<p>
  {{ utils.render(utils.pick(seo.local, seed), r) }}
</p>

<h2>Book Your Transfer</h2>
<p>
  👉 <a href="https://transferise.com">Book private airport transfer</a>
</p>

<hr>

<p>
  <a href="{{ '/' | url }}{{ r.citySlug }}/">
    ← View all {{ r.cityName }} airport transfer routes
  </a>
</p>
